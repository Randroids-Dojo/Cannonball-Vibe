# P1-018 showroom lighting and inspection verification - v37

The interactive car viewer now uses higher quality sky filtering for clear black-paint reflections and removes the temporary show-controls hint when controls return. It supports free orbit, pan, zoom, eleven viewpoints, a turntable, photos, three lighting presets and individual or combined operation of four doors, hood and trunk. Open the parked F2 panel and choose **Explore in showroom**, or use the dedicated scene in the [operating instructions](README.md). The installed geometry remains the production34 review candidate.

`ShowroomStudio` changes only the Sky mode from Realtime to Incremental, retaining its 256 radiance size and existing shader, lighting and material settings. Actual default-mode verification passes 35 cases in Forward+ at 2560x1440 and Compatibility at 1280x720. Independent review of all 16 original captures confirms clearer Forward+ reflection cards. Compatibility still shows grid/moire, hard shadows and weak dark-panel readability; no Compatibility art acceptance is claimed.

A separate counterbalanced Realtime/Incremental native comparison records 87 checks and 8,832 measured post-draw frames. It covers every directed transition between the three presets and settled eight-second motion windows. Incremental images at frame 8 match frame 120. The largest transition-crossing interval is 16.982 ms versus 16.983 ms for Realtime on this shared, VSync-limited RTX 3080 Ti workstation. This is a bounded comparison, not the unresolved Q-047 reference-PC budget. The first OS sampler incorrectly measured the console launcher; its figures are retained but excluded from engine cost claims. The corrected report identifies actual native engine memory/render observations and their sampling limits.

Restoring controls clears only the exact temporary hint. The baseline run reproduces the stale hint, and both corrected renderers pass 41 cases with ten images each. Independent review of six relevant originals confirms removal and preservation of photo notices across view selection. Tiny camera smoothing differences are explicitly retained. The branch preserving an existing non-hint notice during `SetInterfaceVisible(true)` is inspected code; that specific branch is not claimed as an independently exercised native case.

The final actual 1440p modal test passes entry from the parked vehicle, all viewpoints and panel controls, lighting, hidden controls, photos, input suppression, repeated entry and return to driving. It retains 19 direct viewport PNGs and two actual Photo outputs. All 70 recorded inputs match before/after, including the generated runtime scene, GLB, texture files and imported textures, wrapper, setup, presentation data, DLL and fixture. Independent QA viewed all 21 originals; 17 native images and both Photos are byte-identical to the preceding successful run. All eleven preset and three lighting camera states match. Shutdown acknowledges quit, exits zero and leaves no held input, fallback, unfinished work or native diagnostics.

The original detailed screenshot exceeded the automation bridge byte limit. A private debug-only fixture subsequently writes a PNG to one fixed owned directory using native viewport readback after draw, while existing semantic input triggers it. It adds no RPC or arbitrary path and leaves the bridge limit unchanged. Its first layer-zero button failed to receive input behind the modal; the corrected capture button uses layer 128 and hides before readback. The first successful modal run omitted execution asset hashes; a final run binds them explicitly. A guessed import-settings path failed before launch and was corrected from the actual wrapper. All original failures and their recoveries remain retained.

The full Windows front door passes all thirteen steps from 2026-09-13T15:41:08Z to 15:45:10Z after the final code and workflow changes. Its 33 canonical inputs are unchanged. The newly launched 1920x1080, 60 FPS capped Forward+ user showroom has the final DLL, native READY marker and a responding visible game window in its retained snapshot. This does not imply later window lifetime, physical input validation or human approval.

On revision 79fa4ba, both M0 platforms, both long-route jobs, unsigned Windows/Linux checks and PlayGodot Linux pass. macOS passes an unchanged retry after an original graybox warmup shutdown failure; its corrected resize fixture passes all 27 cases. Both Windows PlayGodot attempts end at the outer 15-minute job ceiling, with partial native evidence. Only that Windows job allowance changes to 25 minutes; Linux/macOS remain at 15, and all per-test, eight-second shutdown and M0 gates remain unchanged. Parsed workflow comparison verifies the single changed field. Actual new-revision Windows completion remains required.

Correction to the v36 report: Windows cancellation was attributed to fail-fast. The retained 4865877 and 79fa4ba workflow bytes both explicitly set `fail-fast: false`. The observed job timing and partial execution support the outer job timeout instead. Historical records are preserved.

The [evidence index](../../../data/assets/vehicles/endurance-sedan-review/viewer-v37/index.json) retains 864 files. Both archive builds are byte-identical, all members were read back, and all locked inputs stayed unchanged: 219,623,835 bytes, SHA-256 `0140aae5a4c209f270835d0c7d1ce41ff6fa813d519b63df9f55a45510659cb2`. The prior [recorded walkthrough](showroom-checkpoint-v36.md) remains available as an earlier lighting checkpoint.

The model still needs roof/rear construction, lower-LOD/source normal repairs, final portable source/export integration and full asset/platform/performance verification. Heavy roof/arch forms, coarse mechanical parts, fine surface stippling and dark night doors remain visible. Agent observations support review; art, rights, usability, physical controls and driving-feel gates remain human decisions. P1-018 remains `in_progress`.
