# P1-018 showroom paint and recorded walkthrough - v36

The car viewer provides free orbit, pan and zoom, eleven exterior and cabin viewpoints, three lighting presets, a turntable, photos, and individual or combined operation of all four doors, hood and trunk. Enter through the parked F2 panel's **Explore in showroom**, or launch `res://game/Vehicle/Showroom/VehicleShowroom.tscn`. [The operating instructions](README.md) cover keyboard, mouse and controller controls. The installed model remains the production34 review candidate.

The standard imported material adapter now restores the explicitly authored clearcoat pair. Native inspection had found coat disabled on all 36 painted sedan surfaces despite GLB coat metadata of 1.0 weight and 0.1 roughness. Six real before/after instances and thirteen positive/rejection controls pass; all 224 Hero GT and the single graybox material records remain exact. Changes are limited to the three coat properties. Explicit zero-weight materials also receive their declared inactive values. A wholly absent pair leaves the material unchanged. Incomplete, Boolean, nonnumeric, nonfinite and out-of-range pairs are rejected by the recorded contract. A malformed target is rejected before its coat setters; this is not a global material transaction.

Independent review of twelve actual Forward+ 2560x1440 captures confirms a stronger glossy black finish and reduced visible crosshatching at the two compared views. Cloudy broad hood reflections, heavy roof edges and poorly readable dark panels remain visible. The separate six-image Blender comparison found no broad improvement from clearing the hood's saved custom normals, so no source normal or geometry change was adopted. Later showroom sky-filtering experiments are outside this checkpoint.

The pointer fixture now compares the viewer viewport with the actual positive, changed OS-resolved window size. On macOS, the original 1600x900 request became 1600x645 and the viewer correctly followed it; only the fixture's requested-size assumption failed. The original artifact is retained, along with a predicate replay and stale/unchanged/zero-size rejection controls. All 27 actual Windows pointer cases pass after the correction, including live resize and clean shutdown. This is local evidence; a new native macOS run remains required.

The actual native showroom recording is 137.833333 seconds at 2560x1440. It includes complete closed and all-open rotations, each of the six independent opening/closing sequences, top and underside views, cockpit and rear cabin, engine and luggage inspections, and studio/daylight/night cases. All 46 native cases and 23 direct PNG captures pass. Independent QA viewed 78 original decoded images and confirmed sampled framing, settled panels and compartments, changing wiper positions and alternating front indicators. Several initial cached endpoint samples reached the next camera; twenty separately retained inside-window samples correct that selection limitation. Cached state is not a per-frame motion proof, and sampled images do not certify continuous collision freedom or human usability; actual browser playback is checked separately below.

The original Godot Theora recording retains a warning about keyframe marking. Its complete error-level decode passes, and all 3,200 nonempty frame timestamps match their native packet slots; 935 zero-length packets account for the held pictures. This original remains available as evidence. A separately identified H.264/AAC playback copy preserves those presentation times and the three recorded final held slots. Its exact verification and independent review are bound in the evidence index; it is a delivery transcode of real footage, not another native render.

A real Chromium HTMLVideoElement played the delivery MP4 to its end in137.886 seconds, with no media errors or corrupt frames. It reported30 dropped frames of3,203 (0.937%); all four seeks to8,68,116 and137 seconds completed at the requested time with enough data. Browser display was1233x562 CSS pixels, separate from the native2560x1440 footage; audio stayed muted. This is playback evidence, not a new vehicle frame-time or human usability gate.

The full Windows front door passes all thirteen steps from 2026-09-13T13:05:37Z to 13:09:41Z: 157 C# tests, 345 map tests with one declared skip, 216 automation unit tests, four release tests and required official-engine scenarios. Build warnings and errors are zero. The exact 32 canonical inputs remain unchanged through that run. Native paint, pointer and movie checks use the bound DLL and ordinary acknowledged shutdown without fallback or native diagnostics. Original failed attempts and their bounded recoveries remain retained.

The [retained evidence index](../../../data/assets/vehicles/endurance-sedan-review/viewer-v36/index.json) links the twice-built archive, inventory, command records, original CI artifacts, full-resolution images, recordings and independent reviews. All1,049 retained files were read back; both archive builds are exactly identical (379,905,312 bytes, SHA-256 `e21563536d29ed35f12f1228d0847adaad410ae2f844bede94dfa6c0a30cce89`). Two failed packer schema-key assumptions were corrected before any archive output and remain retained. A snapshot records the newly launched 1920x1080, 60 FPS capped Forward+ showroom's actual process tree, visible window and ready marker. It does not assert later window lifetime or user approval.

On revision 4865877, both M0 platforms, both long-route jobs and Windows/Linux unsigned-package sedan checks pass. PlayGodot Linux passes; macOS has the single resize expectation failure, and Windows is cancelled by fail-fast. Sedan Assets fails on Windows and Linux on four source lower-LOD batches, containing 22 actual zero corner normals; Hero GT passes. The installed GLB's stored normals independently pass, which does not repair the native source. The frozen distant-tire proposal and ongoing roof/C-pillar/rear-cover construction are not adopted here. The patch's new remote evidence, Q-047 performance acceptance, final model acceptance and all required human gates remain open. P1-018 remains `in_progress`.
