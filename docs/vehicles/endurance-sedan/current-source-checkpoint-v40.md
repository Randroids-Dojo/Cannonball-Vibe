# Current source integration checkpoint

P1-018 remains in progress. This checkpoint adds source-generation support for
the revised front and upper assemblies. Their specification/profile selectors
remain unselected pending physical fit, budget, and final artifact verification.
The installed vehicle is still the previously retained prototype.

The current front uses a continuous guide field across the fascia and fenders,
including the manufactured intake and arch surfaces. The upper stage preserves
the existing moving parents and independently binds the formed backlight and
its complete geometry, UV, normal, and material fields. Source generation records
the actual saved predecessor, requested construction, reopened current source,
and helper bytes. Export validation consumes that same chain.

Three measured lower-detail repairs avoid folded meshes: the two front-door
paint groups retain their evaluated original frames before component reduction,
and the rear-left rubber group uses the existing component simplifier. Final
batching still uses the existing rigid parent. The absent-selector path remains
unchanged.

## Recorded Windows verification

The following local evidence is under `reports/p1-018/finish-branch40/`.
These are diagnostic fixtures, not a completed final vehicle generation.

| Evidence | Result and boundary |
| --- | --- |
| `front-native-chain108.json.gz` | Current front900 source, three panels, 17,442 corners, saved/reopened observations and six corruption checks pass. |
| `front-cost127.json.gz` | All six current front lower panels pass; 6,156 triangles total, with 1,108 original mesh fields unchanged. |
| `front-controls117.json.gz` | Full 37 corruption checks and exact restoration pass on the earlier front874 fixture. |
| `upper-native-chain100.json.gz` | Upper883 source observation, regeneration, and seven corruption checks pass. |
| `upper-bake-shell101.json.gz` | All 28 upper components pass before and after their actual rigid-parent bake. |
| `upper-whole-lower-cost122.json.gz` | All lower indexed shells pass; the whole upper fixture exceeds the triangle budget. |
| `upper-native-chain171c.json.gz` | Real base/native/corrected upper chain, all28 actual fields, saved/cold replay and nine negative controls pass. Earlier171/171b harness failures remain retained. |
| `lid-adapter176b.json.gz` | Four source-bound moving-parent distant lids reproduce the measured representation; nine negative controls pass. Combined fit and live transitions remain. |
| `tire-pipeline181.json.gz` | Current44 four-tire construction, actual historical52 predecessor, saved/cold replay, lower-source binding and eight negative controls pass. |
| `tire-qa183.json.gz` | All28 persistent QA controls pass on current44 and all28 on a saved actual historical52 fixture; original four-tire fields restore exactly. |
| `m0-186/summary.json` | All 13 `scripts/check.sh` steps pass: 169 xUnit tests, 345 map tests plus one skip, 216 automation host tests, and the official native probes. This run includes the current upper/tire/lid pipeline edits. |

Blender is 5.1.2 `ec6e62d40fa9`; Godot is official 4.7.1 .NET `a13da4feb`;
.NET SDK is 10.0.102 and uv is 0.9.24. Exact commands, UTC times, input hashes,
exit statuses, and failure/retry logs are retained beside the reports.

Independent QA inspected all 48 actual fixed-camera moving-light front images
in `reports/p1-018/research/upper-junction30-proposal01/front-strip911/`.
The current front softens the earlier jagged lamp-corner reflection and preserves
the visible arch outline. The clustered highlights and enlarged late-sweep lobe
remain subject to full-resolution review; three studio strips contribute to the
reflection. This cropped sequence does not establish whole-vehicle art approval.

## Remaining delivery gates

Actual122 remains the last whole lower-generation census. Composing current
front, corrected upper, current44 tires and four measured distant lids predicts
149,354 LOD0 and 204,978 total triangles. The150,000/200,000 limits stay fixed.
This arithmetic is planning evidence; complete combined native generation is
authoritative. Paint ratios175 offer a further conditional1,652 reduction and
the single FR lower tire17 saves146 in its actual pilot; neither is accepted
for the final package. Further geometry economy and final visual/fit checks
are required. Rejected hardware, inner-wall and cabin trials receive no credit.

The scoped door/frame manufacturing interfaces pass complete independent
stock, departure-gap and original-footprint confinement checks. The common
roof-rail/C closing caps still overlap: exact982 witnesses are approximately
10 micrometres inside C and14.5/26.4 micrometres inside the rail. Scope184
authorizes one complementary cap repair through the existing geometry owner;
it grants no overlap allowance. Whole current-source joint and motion checks
remain required.

Native mirror diagnostics remain incomplete. Hosted managed optical12 checks
pass, but local Windows Application Control blocked the private assembly.
Corrected native camera/marker behavior and final performance remain open.

Final source generation, deterministic double export, clean import, all required
media, performance runs, and final platform checks follow those corrections.
Human art, rights, usability, and driving/physical-control gates remain pending.
PR #144 stays draft; no source promotion or merge eligibility is asserted here.

## Hosted managed diagnostic

The pushed `6107f01` checkpoint passes Linux and Windows M0, all three
PlayGodot platforms, both deterministic long-route jobs, and the unsigned
exports and packaged smoke jobs. Downloaded sedan asset logs from run
`35592892686` confirm that both platforms stop at the first export because
the installed source lacks its current source-generation binding. Remote main
was refreshed to `827bb146` on 2026-09-21; this branch contains that revision.

The revised optical marker test requires each RGB channel to independently
decode the same six-bit pattern. It keeps the existing component, visibility,
pose, and pixel-error guards. Both private builds and 213 source/Python checks
pass, but local Windows Application Control denies loading the test assembly
before any managed cases execute. Those checks are not a managed or native pass.

Scope139 adds a separate, path-limited diagnostic workflow and an immutable
source recipe at `tools/vehicles/diagnostics/p1-018-optical12.zip`. Windows and
Linux runners build the exact sources and execute the existing helper's 66
cases and 100 assertions. Root and independent QA verified all 123 archive
members, 122 manifest inputs, and 112 base Git blobs before publication.
The workflow records its actual runner, revision, commands, and assembly hashes.
Its result does not resolve local Windows trust or establish final rendering,
mirror usefulness, or performance. Existing required workflows and merge gates
are unchanged.

The full Windows `scripts/check.sh` front door passed again after this scoped
workflow addition (`reports/p1-018/finish-branch40/m0-144/summary.json`, all
13 steps). This verifies the current canonical project. The exact frozen helper
subsequently passed on hosted Windows and Linux in run `35596852255`, at PR
head `5ccd642`: both platforms built and loaded the actual assemblies and
passed all 66 cases and 100 assertions. Root verified the merge-test commit,
recipe/workflow Git blobs, all 34 downloaded files and command exit statuses
in `reports/p1-018/finish-branch40/hosted-verification150/verification.json`.
This closes the hosted managed-only check. Local Windows trust and native
rendering/performance remain separate. The same head passes all regular CI
and unsigned-export jobs; the sedan asset jobs still fail before export.

Scope146 separately permits a bounded correction to the two lower stamped-C
inner bands, where complete finite footprints measured original stock below
1.2 mm. The exterior, fixed boundaries, topology, and physical tolerances stay
fixed. This correction and the new distant-lid budget trials remain unpromoted.


## Current geometry and quality checkpoint

The private combined upper964 corrects roof/C stock and the paired fore-frame
sections. Native171c binds all28 final members through real base, native
intermediate and corrected stages. It saves/reopens the current source and
independently regenerates exact fields. No source selector or export is promoted.

Independent QA's departure07 and confinement09 reports cover eight installed
and four actual moving-parent interfaces. Minimum complete scoped stock is
1.201098 mm; all eight terminal sections have at least1.010447 mm gap. Complete
finite confinement checks retain all original581 receiving-foot triangles:
15,006,432 surface pairs,1,049 intersections and2,332 actual Boolean triangles
are accounted for, with zero strict remainder outside the declared physical
domains. Tapered departures are not new penetration targets. These results
close the scoped attachment interpretation; the common rail/C joint, continuous
motion and final combined source remain separate.

Current44 tires pass actual979 controls at all four corners and native181
construction/checkpoint/save/cold replay. Their total is16,560 triangles,
1,328 fewer than the genuine historical52 stage. Independent QA inspected all
eight static972 and48 rotation976 originals. No broad shoulder, outline or
seating defect was observed; the extreme bead closeup is coarser. These are
sequential diagnostic stills, not final full-turn gameplay acceptance.

Actual paired hardware157/158 renders reject the cheaper vent and door fixed
mount ends because their visible bevels disappear. No A40 saving is credited.
The inner-wall pilot adds a3.18 mm body penetration and is rejected. Cabin13
gives only68 conditional triangles and creates an approximately9.90 micrometre
console/tunnel penetration; that package is rejected. The narrower current
LOD2 FR tire17 keeps all existing wheel/brake groups and measures146 triangles
saved; full rim/field/distance review and the other corners remain open.

The distant lids pass independent review of twelve actual1440p images at28 m
and65 m. Finite164 covers all4,276 faces in both directions,10,751 disjoint
pieces and zero remainder. Original-field correspondence bounds are0.292016 mm,
0.618294 degrees and0.000243538 UV for the hood;0.299228 mm,1.142219 degrees
and0.000293331 UV for the trunk. Independent finite replay passes. The
source-owned172 adapter reproduces all four native representations in176b,
preserving original meshes and using their original moving parents. Its1,016
lower-triangle saving still needs combined-source generation and live imported
LOD transitions. Fine rear-highlight visibility in static comparisons is limited.

The requested `main-loop-handoff.md` comparison is covered by the existing
research geometry owner on the front panels: actual identical-geometry normal
comparisons in `front795/`, matched context/grazing/neutral/arch/intake pairs in
`front895/`, and 24 before/after moving-light pairs in `front-strip911/`, all
under `reports/p1-018/research/upper-junction30-proposal01/`. Root and independent
QA reviewed the retained images. Local field defects improve with the arch
outline retained; the lamp-shoulder fork, lower-lip endpoint and broad upright
fascia remain explicit form-review limitations. This comparison does not grant
whole-car or human art approval and does not introduce another constructor.

The reusable coverage and original-LOD-field lessons are published to the
skills repository at `dda70d67cad47c261669a3f74b077ef9e398787f` and reinstalled
globally through the normal installer. All 16 installed files match that
committed source; local validation and both hosted skill workflows pass.
The complete receipt is `reports/p1-018/skill-update151-01/completion.json`.
