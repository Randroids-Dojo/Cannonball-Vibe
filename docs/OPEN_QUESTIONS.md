# Open questions

This register contains unresolved choices that can materially change scope,
architecture, validation, or release. Defaults permit progress; they are not
final answers. Close a question by recording evidence and linking the resulting
ADR, GDD decision, or delivery-ledger change.

The current graphics-foundation handoff is
[QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md)
(Q-037 to Q-041). The current multiple-choice handoff for Q-022 is the
[2026-08-13 High-profile handoff](QUESTIONS_FOR_RANDROID_2026-08-13_Q022_HIGH_PROFILE.md),
which supersedes Q-022a, Q-022b, and Q-022e in the
[2026-08-03 reference-capture ratification handoff](QUESTIONS_FOR_RANDROID_2026-08-03_Q022_REFERENCE_CAPTURE.md)
and leaves Q-022c and Q-022d there unchanged. Both supersede
[the original target handoff](QUESTIONS_FOR_RANDROID_2026-07-18_AUTONOMOUS.md)
and the [systematic-backlog handoff](QUESTIONS_FOR_RANDROID_2026-07-18_SYSTEMATIC_BACKLOG.md).
The exact source-release publication handoff is
[Q-027](QUESTIONS_FOR_RANDROID_2026-07-19_SOURCE_PUBLICATION.md).
Q-028 and Q-029 were approved on 2026-08-13; their handoffs
([trip map](QUESTIONS_FOR_RANDROID_2026-07-19_TRIP_MAP.md),
[camera comfort](QUESTIONS_FOR_RANDROID_2026-07-20_HANDLING_GATES.md)) are
retained as history. See the
[approval record](audits/2026-08-13-q028-q029-owner-approvals.md).

| ID | Priority | Blocking milestone | Owner | Question | Working default | Evidence required |
| --- | --- | --- | --- | --- | --- | --- |
| Q-046 | P1 | M3 | Design + runtime | What stock starter speed cap, upgrade increments, engine overheating thresholds, brake-fade floor/recovery, and driving-dependent tire wear rates satisfy the [2026-09-06 design requirements](../gdd.md#starter-speed-heat-and-wear-requirements)? | P1-017 implements the stock cap at a provisional 125 mph with a grounded forward governor and 1 mph power taper. Upgrade progression, overheating, severe brake fade, tread wear, and the distinction between cooling and lasting damage remain required future work; balance values remain unratified. The previously discussed 120–130 mph starter and 150/175/200 mph upgrade examples are tuning candidates only. | Measured acceptance comparisons listed in the GDD, a winnable stock configuration, and human approval of driving enjoyment and warning/recovery comprehension. P1-017 owns technical stock-cap verification only; claim separate delivery-ledger slices for the remaining systems and human gates. |
| Q-045 | P1 | M3 (production atlas) | Runtime + map pipeline (P1-016) | Does the pilot support adopting [proposed ADR-0027](decisions/ADR-0027-streamed-highway-atlas.md), including independent native map tiles and newly cataloged non-road context, and can the complete real exit/service coverage promised for the selected highways be substantiated? See the [design and source gaps](audits/2026-09-06-streamed-highway-map-design.md) and the [ADR-0028 intake pipeline](../data/atlas/README.md), which reports source blocks and unknown inventories without closing this question. | Proceed with the reviewable design and scoped fixtures. Recommend local content streaming and native Godot/FlatBuffer cartography. Do not admit unreviewed sources, broaden TIGER road use, invent absent real-world fields, or claim complete coverage. | Urban/mountain/rural pilot artifacts and size/latency measurements; per-segment exit/service inventories with independent denominators; exact source rights and ancestry dispositions; ADR acceptance; new atlas comprehension/accessibility review before production completion. |
| Q-041 | P2 | M5 | Runtime + art (P1-013) | The project defaults are now the Balanced renderer tier because the reference-PC settings cancelled Windows M0 at its timeout on the software-rendered runners; `RenderQuality.Apply` raises the game to the High tier (4x MSAA, 8192 shadow atlas, soft-medium filtering, medium SSAO) only when it starts with `--environment-quality=high`. How does the shipped build select High: a settings-menu entry, GPU detection on first run, or High by default with a CI override? See [the 2026-09-02 handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-041---renderer-tiers-and-how-the-shipped-build-selects-high). | A settings-menu entry in P1-013; automation keeps the argument. | Owner answer, then a P1-013 note or ADR-0023 addendum. |
| Q-040 | P1 | M5 | Art (P1-008) | Q-020 chose the project-original Hero GT, and with the environment now textured the beveled-box car is the weakest surface on screen. Does the production vehicle stay procedural (a substantial remodel next pass), get commissioned or licensed under a clear licence and adapted to the existing semantic rig, or both? See [the handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-040---hero-gt-production-art-path). | Continue procedural; no third-party car enters the repository. | Owner decision; a licensed model additionally needs its own Q-023 rights record. |
| Q-044 | P2 | M5 | Runtime + art (P1-013) | The third-generation Hero GT carries 108,039 LOD0 and 124,395 total triangles, 26 materials, 27 textures and a 31.9 MB GLB; the exporter records 150k / 200k triangles, 32 materials, 48 textures and 96 MB as representative ceilings because ADR-0012 left them open until an asset existed. Are these ratified in P1-013 with a measured Forward+ frame time, trimmed to the open-world band, or kept provisional until traffic vehicles share the budget? See [the 2026-09-02 handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-044---hero-gt-budgets-and-payload). | Ratify in P1-013 with a measured frame time. | Owner answer, then a P1-013 note or ADR-0023 addendum. |
| Q-043 | P1 | M5 (release inclusion) | Owner | Ten CC0 texture sets for the Hero GT (ambientCG, TextureCan, Poly Haven) are locked in `data/assets/vehicles/sourced-materials.lock.json` as `pending-human-review`; the packer redirects their runtime copies to `assets/vehicles/sourced/hero-gt/`, which release presets exclude, so a release build loads the car untextured. Approve all, approve per record, or reject sourced textures for the vehicle? See [the 2026-09-02 handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-043---rights-review-for-the-hero-gt-texture-sets-blocking-release-inclusion). | Approve all ten and lift the exclusion. | Lock statuses flipped by the owner and the exclusion removed in one PR. |
| Q-042 | P2 | None (live-suite reliability) | Runtime + automation | `test_pause_clears_held_input_until_neutral` failed again on macOS on 2026-09-02 (PR #108 run 33654647496, job 100330041261): `wait_for_conditioner` timed out with "Pause did not suppress held input" while the conditioner still reported `stationary_hold` false and no suppression, so the pause never reached the conditioner inside the 2 s wait rather than reaching it incoherently as in the Q-035 case. The fix for the Q-035 pause class made the suppression publication atomic; this is either a second cause in the same test (the pause request itself landing late on a loaded macOS runner) or a bound that the runner cannot always meet. The PlayGodot job is not a merge gate, and the other two runners passed on the same commit. Second macOS failure the same afternoon on PR #109 (run 33655554915, job 100333100432): `test_controller_camera_recover_menu_and_confirmed_restart_are_distinct` asserted `elapsed_seconds < 1` after a confirmed restart and read 2.306 s while every exact field of the restart (distance, position error, rotation, speeds) matched; that bound measures describe latency on the runner, not the restart. Two distinct tests on two consecutive macOS runs suggest the macOS runner got slower rather than that either test regressed; the Windows and Ubuntu runners passed both times. Root cause for the `elapsed_seconds` bound, found 2026-09-02 after a third failure at 1.18 s on PR #112: `RestartRun` started the session clock before `ConfigureRuntimeWorld` rebuilt the streamed world, so the rebuild's wall time counted as run time, and the P1-010 environment made that rebuild slower than a second on the macOS runner. The clock now starts after the rebuild on the restart, resume and long-route paths, which is also the authoritative-clock semantics Q-019 set for pauses. The pause-wait and camera-handling timeouts remain unexplained. Should those be root-caused as a fifth live-suite class in the Q-035 style, or is the macOS runner to be dropped from the live suite? Recurrence 2026-09-04 on the first run after the third-generation Hero GT (run 33900099664): Windows `test_camera_handling_survives_pause_device_reset_and_mode_transitions` timed out on its first `ui.describe` at 30 s (job 101111896503) and macOS `test_repeated_authentication_failures_trigger_bounded_cooldown` and `test_hostile_requests_fail_closed_and_are_transcribed` missed their 2 s socket windows (job 101111896558), while Ubuntu passed. Cause: the model brought twenty-eight textured materials whose first draw compiles shaders on the software renderers and stalls the main thread past those windows. The launcher now runs the graybox vehicle for the suite, as it already runs the graybox environment, and only the camera test loads the production rig with a 150 s first-draw budget; no bound moved. Second pass the same day (run 33901250367): the raw-socket helper in `test_live.py` builds its own launch and had not received the flag, so the cooldown and hostile tests still loaded the production car (macOS twice, Ubuntu once); and the Windows damping test read a qualifying 1.56 degree sample on the describe that arrived after its 1 s deadline, which the loop checked first. Both fixed without moving a bound. The camera test took 83 s on Windows, 51 s on Ubuntu and 35 s on macOS against the production rig. Recurrence 2026-09-04 on the lamps PR (run 33907041405, macOS): the camera test read a rear-view blend of 0.85 against its 0.9 bound at the end of the settle window; cause: two shadowed spot lights had just been added under the head anchors and the test renders the production rig on the software renderer. Beams cast no shadow under the graybox-assets flag now; no bound moved. Recurrence 2026-09-04 on the lamps follow-up (run 33907873308, macOS, job 101137028350): `test_hostile_requests_fail_closed_and_are_transcribed` timed out on its first `ui.describe` after reconnecting; the transcript shows the reconnect `session.hello` succeeded and the describe was never processed, with the graybox vehicle and no lamps in the process. That is the Q-035 hostile-request bridge class (a fresh client accepted while the bridge still holds the previous peer), its third recorded occurrence on macOS, and it is independent of the vehicle; root-causing it needs the bridge server, not the rig. Recurrence 2026-09-04 on the dash PR (run 33908478458, macOS): the camera test read 0.842 of its 0.9 rear-view bound with the beam shadows already off, so the shadows were not the cause: the rig's blends advance per rendered frame (`RearViewTransitionSharpness` 14 against a process step the engine caps), and the production rig on the macOS software renderer draws too few frames inside the 3 s settle window as the model grows (228 to 231 nodes today). Fix in the Q-042 spirit: the camera test runs the graybox car like the rest of the suite, and the production rig's cockpit contract (exclusion names on the exterior layer, cull masks, sourced-texture binding) moved to the headless `camera-handling` scenario that M0 gates on every PR; no bound moved. | Root-cause in the Q-035 style before touching a bound; log further recurrences here. | A recurrence count with run links, then an audit entry with the cause. |
| Q-039 | P1 | None (local verification) | Automation | Windows Smart App Control on the reference PC blocks every newly written unsigned binary (Code Integrity events 3077/3118, verified 2026-09-02): a fresh worktree venv cannot load `pyogrio`, and uv regenerating the shared venv's `cannonball-map.exe` produced a blocked launcher in `C:\Dev\Cannonball-Vibe\tools\map_pipeline\.venv`. `run-scenario.sh` and `capture-scenario.sh` now call `python -m cannonball_map`; `capture-reference-performance.sh` and `validate-continental-route.sh` still call the launcher. Switch them too, change the machine policy, or keep one trusted venv for all worktrees? See [the handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-039---windows-smart-app-control-on-the-reference-pc). | Point worktrees at the trusted venv with `UV_PROJECT_ENVIRONMENT`, `UV_NO_SYNC=1` and `PYTHONPATH`; never let uv sync the shared venv from another worktree. | Owner decision and, for A, a one-line change in each script's owning task. |
| Q-038 | P2 | None (CI cost) | Automation | The sourced art adds 61 MB to Git LFS and six workflows check out with `lfs: true`, so every run downloads it against GitHub's 1 GB per month free bandwidth. Accept and monitor, cache LFS objects in CI keyed on the lock hash, or shrink the payload? See [the handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-038---git-lfs-budget-for-sourced-art). | Accept and monitor for a week. | LFS usage panel reading or an owner choice. |
| Q-037 | P1 | M5 (release inclusion) | Owner | Sixteen Poly Haven CC0 records in `data/assets/environments/sourced-assets.lock.json` are `pending-human-review`; Q-023 requires an individual human rights review before any moves to `approved`, and until then `assets/environments/sourced/` is excluded from both release presets so exported builds fall back to flat colours. Approve all, approve per asset, or reject the provider? See [the handoff](QUESTIONS_FOR_RANDROID_2026-09-02_GRAPHICS_FOUNDATION.md#q-037---rights-review-for-the-sourced-poly-haven-assets-blocking-release-inclusion). | Pending; nothing ships sourced art until answered. | Owner approval recorded in `docs/QUESTIONS_FOR_RANDROID.md`, the lock statuses flipped, and the preset exclusions removed. |
| Q-036 | P3 | None (source metadata quality) | Map pipeline | A HEAD census during the P0-021 corridor elevation acquisition found TNM's catalog `sizeInBytes` disagreeing with the immutable dated staged S3 objects for exactly 6 of the 124 locked corridor 3DEP products: two sub-megabyte nonsense declarations (n36w102 361,416 B and n36w103 338,581 B claimed for ~350 MB tiles) and four percent-scale staleness errors (n36w104, n38w080, n42w086, n42w087). Every affected object was last modified 2022-12-03 - years before the product lock's discovery snapshot - and live re-discovery still asserts the defective values (items lastUpdated 2026-06-08), so the defect is the catalog index, not object drift; all six rasters verify fully (CRS, cell size, band layout, nodata, coverage) and the S3 objects' own Content-Lengths match the downloads byte for byte. The acquisition refuses any unpinned mismatch and holds these six tiles to characterised pins (measured bytes plus SHA-256, strictly tighter than the defective declarations) in `ELEVATION_DECLARED_SIZE_EXCEPTIONS`; see the [corridor elevation audit](audits/p0-021/2026-08-31-corridor-elevation.md). Should the defect be reported upstream to USGS, and should a pin be retired to plain byte equality if a later catalog refresh corrects its declaration? | Keep the six hash pins; each is stricter than the declaration it stands in for. Any new mismatch outside the pinned set stays a refusal, never a silent widening. | A future re-census showing corrected catalog declarations, an upstream report/fix, or an owner decision closes this row. |
| Q-033 | P2 | None (merge-gate reliability) | Runtime + automation | `SubsystemProfilerTests.NestedRegionIsNotChargedToBothSubsystems` asserts a wall-clock duration in range 16–90 ms and measured 107.29 ms on a windows-latest runner during PR #85's CI ([run 33120558335](https://github.com/Randroids-Dojo/Cannonball-Vibe/actions/runs/33120558335)); it passed unchanged on re-run. This test runs inside the required M0 gate, so its flakiness can hold any PR's auto-merge (ADR-0025). Should the profiler tests inject a controllable clock instead of asserting wall-time ranges, or should the bounds widen for shared CI runners? | One re-run restores the signal. Because this flake sits in a required merge gate, a second occurrence anywhere escalates immediately to a repair task; the presumptive fix is a fake/injectable clock in `tests/Cannonball.Core.Tests/SubsystemProfilerTests.cs`, preserving the charged-once semantics without timing sensitivity. | Recurrence log (dates, run links, measured durations) in this row; a second occurrence or an owner decision triggers the repair. |
| Q-022 | P1 | M5 | Runtime + art (P1-013) | Do representative production measurements validate the provisional reference limits, and what subsystem/content-class allocations should be ratified beneath them? | At 2560×1440 High on the declared Windows PC, require p95 ≤16.67 ms, p99 ≤20 ms, no steady-driving stall >50 ms after warm-up, GPU memory ≤9.5 GB, process working set ≤16 GB, and no sustained growth over 30 minutes. Collect evidence through an owner-started Codex session running directly on Windows and the native RTX 3080 Ti renderer, using persistent pinned tools/caches and a fresh clean worktree per capture; see [ADR-0023](decisions/ADR-0023-reference-performance-target-and-layered-budgets.md). | The High content-profile capture recorded on 2026-08-13 at revision `de42c82fdd6519895b9164c6d8a11a4f08bd7015` satisfies the High **content**-profile recapture requirement the [balanced baseline](audits/2026-08-03-q022-reference-windows-capture.md) left open; the separate High **renderer**-preset gap stays open because the repository still defines no such preset. Percentiles, GPU memory, and working set pass with 8.6× p95 and 6.7× p99 headroom; the 30-minute run passes the **proposed** sustained-growth rule at 0.56 MiB/min with R² 0.16, which Q-022d has not ratified. The zero-stall limit **fails in four scenarios** (daylight, high-speed, streaming, steady-state-30m) across 12 steady-driving events; three identical streaming runs produced 4, 0 and 0 events, so the failure is intermittent and unattributed. Raising balanced→High did not measurably raise percentile frame time, so neither capture can price environment density. A V-Sync-on presentation check now exists (119.95 FPS, zero stalls) but at 120 Hz, not the 60 FPS target. See [the High-profile capture audit](audits/2026-08-13-q022-high-profile-capture.md). A [repeat of the same nine scenarios](audits/2026-08-13-q022-high-profile-repeat-capture.md) eleven hours later recorded **zero** stalls at identical arguments and content, so the failure is not reproducible. That is consistent with a machine-state, contention, or measurement-method cause but excludes none of them, and does not exclude an intermittent content, code, driver, or instrumentation cause, since such a cause need not appear in any given matrix. One repeat run showed mean render GPU time about ten times its siblings at identical arguments, showing that a capture's GPU timing can change by an order of magnitude on a metric the workspace policy does not observe. Q-022 stays open for owner treatment of the stall failure and budget/method ratification; traffic, weather, measured per-subsystem attribution, and a repository-defined High renderer preset remain absent. A [60 FPS cap presentation check](audits/2026-08-13-q022-presentation-60fps-check.md) now exists (engine frame cap, not a 60 Hz output mode): the build holds the cap at 60.009 mean FPS with zero stalls, but the run records p95 as failed at 16.9075 ms. The 16.67 ms limit sits 3.3 microseconds above a 60 FPS cap period, so it is attainable in principle yet permits only 3.3 microseconds of p95 jitter; measured jitter was about 241 microseconds. Q-022e must decide whether to restate the limit for capped runs. |

## Recently resolved

| Decision | Resolution |
| --- | --- |
| Q-035 PlayGodot live-suite stabilization | Recurrence of the pause class logged 2026-09-02 as Q-042. Closed 2026-08-31 by the escalated suite-wide stabilization pass the working default demanded once a fourth distinct live-suite test flaked (`test_hostile_requests_fail_closed_and_are_transcribed`, windows, red-main #100). All four flake classes were root-caused rather than tolerated or quarantined, and no assertion threshold moved: the deadzone flake was a fixed sleep sampling after the vehicle had legitimately decelerated past the brake-to-reverse handoff (now a bounded wait on the first braking sample, from a wider 6 m/s runway); the pause flake was a non-atomic suppression publication in `DrivingInputController.ClearAndSuppress` (now atomic, and the test waits for the full suppressed steady state); the hostile-request NoneType was the bridge dropping an immediately-reconnecting client while its stale previous peer still reported connected (bridge now polls the stale peer at accept; the raw-request helper retries connection turnover within a bounded deadline). Every remaining live test was audited for raw sleeps before positive assertions, unsettled physics sampling, and read-after-write races, and converted to bounded waits on observable transitions with diagnostics on timeout. Verified by three consecutive green end-to-end local runs at unchanged suite duration. See the [stabilization audit](audits/2026-08-31-playgodot-live-suite-stabilization.md). |
| Q-032 trip-map resume vertical stability | Folded into the Q-035 stabilization pass and closed 2026-08-31 with a root cause: the exceedances were a real game bug, not a tight bound. The vehicle subtree runs in `ProcessMode.Always`, so it kept applying per-tick suspension/drive forces while `GetTree().Paused` deactivated the physics server; the forces accumulated for the whole pause and integrated as one impulse on resume, scaling with wall-clock pause length. Measured locally: a 2 s pause produced an 11.37 m/s upward resume impulse before the fix and 0.52 m/s after (`CannonballVehicle._PhysicsProcess` now applies no forces while paused). The 2.0 m / 5.0 m/s bounds are unchanged and now hold with ~27x / ~10x margin; the test's post-resume watch settles deterministically instead of sampling a fixed wall-clock window. See the [stabilization audit](audits/2026-08-31-playgodot-live-suite-stabilization.md). |
| Q-034 continental corridor breaks | Closed 2026-08-31 after three slices: the per-site ADR-0018 disposition of all 14 bounded break sites, the lock revision (5 scoped NHPN supplementary acquisitions resolving Q-034a/b, 5 checksum-locked ADR-0026 NHS fills, re-derived transfer/edge-path locks), and the topology closure ([audit](audits/p0-021/2026-08-31-topology-closure.md)): Q-034c/d resolved by the edge-split-at-anchor mechanism (the edge-path solve splits a locked record at an on-edge anchor as a fallback beyond the unchanged 25 m endpoint search - both anchors resolved at 0.000 m offset with the ADR-0024 transfer lock byte-identical; anchor re-derivation was rejected for moving research-grounded coordinates 381/253 m onto segmentation artifacts), and the two bounded micro-gap exceptions (Omaha 1.011 m, Quad Cities 9.190 m) authored through the new ADR-0018 reconstruction gates into [reconstruction-overlay-lock.v1.json](../data/routes/continental/reconstruction-overlay-lock.v1.json) with endpoint/length/source-adjacency/self-intersection gates enforced, geometry-stage gates explicitly deferred, and the Quad Cities 77.2-degree interchange-corner heading recorded as that stage's authoring constraint. **12 of 12 segments chain anchor-to-anchor** (8 pure-NHPN; 6,294.1 mi continuously locked/chained). The disposition record is `lock_revision_implemented_topology_closed`; chains are undirected and mixed-ancestry - no westbound selection, no authoritative distance, tolerances unchanged. Remaining corridor work (NHPN-NHS conflation, 3DEP, westbound selection, reconstruction geometry, runtime, human gates) is tracked by P0-021 itself. |
| Q-029 camera comfort and readability | Approved by the owner on 2026-08-13 after reviewing chase and cockpit renderer captures of the integrated visual slice: "The views are fine for today, but I reserve the right to tweak more later." Recorded as an approval, with later polish raised as new work rather than a reopened gate. P0-017, P0-018, and P0-019 are complete. The reviewed cockpit is the accepted graybox placeholder; production cockpit art remains P1-008 scope. See the [approval record](audits/2026-08-13-q028-q029-owner-approvals.md). |
| Q-028 trip-map comprehension | Approved by the owner on 2026-08-13 from a full-screen trip overview capture: "Map looks good." Recorded on the same terms as Q-029. P0-013 is complete. See the [approval record](audits/2026-08-13-q028-q029-owner-approvals.md). |
| Q-022a intermittent stall treatment | Answered by the owner on 2026-08-13 as Option A: add instrumentation rather than repeat black-box runs. The reference harness now samples GC generation counts and streamer build, removal, rebase, failure, and chunk-count counters per frame and attaches their delta to every stall, with run-level coincidence totals. The zero-stall threshold stays failed until the cause is found. |
| Supplementary route-geometry sources | The owner directed adoption of the best public-domain sources on 2026-08-30 after the milepost-gap probe proved NHPN cannot supply 2,400.9 corridor miles. [ADR-0026](decisions/ADR-0026-supplementary-route-geometry-sources.md) adopts NTAD NHS as supplementary centerlines, admits NAIP as break-adjudication evidence, stages HPMS/ARNOLD as the deepening path, and rejects North American Roads, USGS NTD, and state DOT feeds. See the [source research](research/2026-08-30-supplementary-route-geometry-sources.md). |
| Q-031 continental route selection | The owner directed route research and selection on 2026-07-31. [ADR-0024](decisions/ADR-0024-canonical-continental-route-graph.md) locks a westbound Central Rockies canonical path, Northern Plains and Southern I-40 major alternatives, historical reference addresses, and adjacent public-road endpoint portals. Exact acquired geometry, authoritative distance, rights review, and complete-drive evidence remain P0-021 work. |
| Prototype engine | Godot 4.7.1 .NET; see [ADR-0004](decisions/ADR-0004-godot-4-7-1.md). |
| Runtime route format | FlatBuffer index and independently hashable chunks; see [ADR-0002](decisions/ADR-0002-public-domain-geodata-contract.md). |
| Fully agentic acceptance | Machine gates may be autonomous; subjective, physical, legal, credential, and release gates require humans; see [ADR-0003](decisions/ADR-0003-agentic-delivery-contract.md). |
| Legacy PlayGodot engine fork and debugger transport | Retired; the modern debug-only addon runs on official Godot 4.7.1 and requires no engine fork; see [ADR-0005](decisions/ADR-0005-official-engine-agentic-automation.md). |
| Q-011 PlayGodot adoption | Activated for interactive rendered-UI surfaces after the representative driver-menu comparison exposed focus and state unavailable through Computer Use. It remains debug-only and absent from release packages; see [ADR-0008](decisions/ADR-0008-required-playgodot-after-ui-value-gate.md). Decision activated 2026-07-19. |
| Q-013 source rights | Approved for public distribution of the exact checksum-locked NHPN and USGS 3DEP government data and project derivatives, with source credit, provenance, a no-endorsement statement, and no agency logos. See [ADR-0007](decisions/ADR-0007-tiered-elevation-and-locked-source-distribution.md). Human approval recorded 2026-07-16. |
| Q-014 production elevation | Use seamless 1/3 arc-second 3DEP as the deterministic required baseline. Permit a locked 1-meter upgrade per corridor only when coverage, seam, visual/grade value, package-size, and acquisition-time gates pass; see [ADR-0007](decisions/ADR-0007-tiered-elevation-and-locked-source-distribution.md). Decision recorded 2026-07-16. |
| Q-015 immutable source retention | Use content-addressed immutable GitHub Release assets as the authoritative primary store; deterministically split artifacts that exceed the per-asset limit and keep local/CI copies as caches only. Independent disaster recovery is conditional under ADR-0010. See [ADR-0006](decisions/ADR-0006-github-release-source-retention.md). Decision recorded 2026-07-16. |
| Q-016 independent recovery replica | Defer provisioning while retained sources remain public, checksum-locked, and reliably reacquirable. If the activation threshold is met, use the ADR-0009 S3 Object Lock design and its required read-only Vercel health plane. See [ADR-0010](decisions/ADR-0010-defer-independent-source-replica.md). Decision recorded 2026-07-16. |
| Q-023 asset rights policy | Permit project-original, CC0, and clearly redistributable assets only after an exact manifest and human rights review. Pending or restricted records remain fixture-only or excluded. The project owner approved the policy and exact graybox fixture rights record on 2026-07-18; future assets still require individual review. |
| Q-025 | P0-012 representative lane/interchange validation corpus geographic plausibility review | Approved 2026-07-19 as Option A in [QUESTIONS_FOR_RANDROID_2026-07-18_SYSTEMATIC_BACKLOG.md](QUESTIONS_FOR_RANDROID_2026-07-18_SYSTEMATIC_BACKLOG.md#q-025--p0-012-geographic-plausibility-and-route-choice-review). |
| Q-027 | P1-006 source publication and immutable release | Approved 2026-07-19 as Option A in [QUESTIONS_FOR_RANDROID_2026-07-19_SOURCE_PUBLICATION.md](QUESTIONS_FOR_RANDROID_2026-07-19_SOURCE_PUBLICATION.md#q-027--publish-the-authoritative-immutable-source-release). Published as `source-lock-v1-9a8a9aa07bc8e4f2`; P1-006 is now complete. |
| Q-021 route-specific regional environments | Approved 2026-07-23 as Option A: pursue grounded realism for every traversed state, with multiple route-specific subregion profiles where the real geography changes. Colorado remains a representative technical slice, not the coast-to-coast art template; see [ADR-0015](decisions/ADR-0015-route-specific-state-and-subregion-environments.md). |
| Q-020 project-original Hero GT | Approved 2026-07-23 as Option A: develop the project-original Hero GT and existing semantic rig into the production vehicle. Final silhouette, cockpit/chase visibility, materials, damage presentation, renderer budgets, and rights evidence remain P1-008 acceptance gates. |
| Q-024 state-specific highway realism | Approved 2026-07-23 as Option A: production roads, signs, structures, markings, and roadside assets pursue contemporary, state-specific American highway realism along the actual route; see [ADR-0016](decisions/ADR-0016-state-specific-highway-visual-language.md). |
| Q-019 solo trip-map pause | Approved 2026-07-23 as Option A: opening the full-screen map in solo play pauses driving and excludes the paused interval from the authoritative run clock. Competitive or challenge modes may use separate explicitly communicated rules later. |
| Q-026 phased undivided two-way highways | Approved 2026-07-23 as Option A: prove divided mainlines, one-way ramps, handling, and traffic first; add explicit undivided two-way cross-sections and opposing-traffic behavior later under P1-011 rather than inferring them in the existing lane model. |
| Q-001 defining run-length mode | Approved 2026-07-23 as Option A: 1:1 Endurance is the defining primary coast-to-coast experience; shorter Standard and Challenge modes remain available from the same authoritative route distance and content. See GDD decisions D-009 and D-013. |
| Q-010 activity density | Approved 2026-07-23 as Option A: target a meaningful speed, route, stop, or risk decision every few minutes on average, with deliberate quiet highway stretches for contrast and authentic pacing. See GDD decision D-013. |
| Q-017 authoritative route-context sources | Approved 2026-07-23 as Option A: use checksum-locked authoritative public records first, then deterministic authored overlays with recursive provenance; never infer false lane, exit, destination, or milepoint precision from coarse topology. See [ADR-0017](decisions/ADR-0017-authoritative-route-context-and-concurrency.md). |
| Q-018 readable route concurrency | Approved 2026-07-23 as Option A: preserve every signed route identity, show the locally primary designation roadside, and expose all concurrent identities and numbering behavior in the map and semantic inspection surfaces. See [ADR-0017](decisions/ADR-0017-authoritative-route-context-and-concurrency.md). |
| Q-002 NHPN route-backbone scope | Approved 2026-07-23 as Option A: retain NHPN only as the public-domain national route-family backbone and reconstruct playable geometry from better evidence and validated corrections. See [ADR-0002](decisions/ADR-0002-public-domain-geodata-contract.md) and [ADR-0018](decisions/ADR-0018-gated-generated-road-reconstruction.md). |
| Q-003 generated correction policy | Approved 2026-07-23 as Option A: generate road and interchange corrections deterministically, accept only candidates that pass strict geometry and driving gates, and use provenanced authored overlays for the exceptions. See [ADR-0018](decisions/ADR-0018-gated-generated-road-reconstruction.md). |
| Q-004 continental package ceilings | Approved 2026-07-23 as Option A: retain a root index below 64 MB and independently hashed shipping chunks below 16 MB until 500-mile and coast-to-coast telemetry demonstrates a reason to tighten them. See [ADR-0019](decisions/ADR-0019-route-package-budgets-and-audit-reproducibility.md). |
| Q-008 GeoPackage reproducibility | Approved 2026-07-23 as Option A: shipping FlatBuffers remain byte-reproducible, while non-shipping GeoPackage audit artifacts require semantic equivalence of IDs, geometry, CRS, attributes, provenance, and content version. See [ADR-0019](decisions/ADR-0019-route-package-budgets-and-audit-reproducibility.md). |
| Q-006 physical wheel baseline | Approved 2026-07-23 as Option A: use the owner-available Logitech G923 Xbox/PC wheel and three-pedal set on the declared Windows 11 PC for the first physical calibration and handling baseline. Force feedback remains optional until basic wheel input passes. See the [device record](audits/2026-07-23-g923-wheel-baseline.md). |
| Q-009 macOS platform timing | Approved 2026-07-23 as Option A: Linux and Windows remain required delivery platforms; continue macOS engineering validation where available without committing to public macOS distribution until a later promotion decision. See [ADR-0020](decisions/ADR-0020-required-platforms-and-macos-validation.md). |
| Q-007 release channels | Approved 2026-07-23 as Option A: Steam is the authoritative commercial customer channel, itch.io may carry labeled demos or early builds, and GitHub Releases remains the technical artifact and provenance channel. No fee, storefront, credential, upload, or publication action is authorized by the decision. See [ADR-0021](decisions/ADR-0021-phased-commercial-release-channels.md). |
| Q-012 automation boundary | Approved 2026-07-23 as Option A: keep command-line and filesystem evidence authoritative, use modern PlayGodot for stable semantic rendered-UI access, and use Computer Use as a black-box layer. No MCP adapter or custom Godot fork is required. See [ADR-0022](decisions/ADR-0022-host-neutral-agent-automation-without-required-mcp.md). |
| Q-022 performance target and budget method | Approved 2026-07-23 as target Option A and budget-method Option A: target 2560×1440 High at a stable 60 FPS on the declared Windows reference PC, using provisional then production-ratified layered budgets rather than averages alone or universal per-asset caps. Subsequent owner choices established provisional numeric thresholds; see [ADR-0023](decisions/ADR-0023-reference-performance-target-and-layered-budgets.md). |
| Q-022 provisional frame-pacing and memory thresholds | Approved 2026-07-23 as frame-pacing Option A and memory Option A: require p95 ≤16.67 ms, p99 ≤20 ms, no steady-driving stall >50 ms after warm-up, GPU memory ≤9.5 GB, process working set ≤16 GB, and no sustained growth over 30 minutes. Representative Windows capture and derived allocations remain open; see [ADR-0023](decisions/ADR-0023-reference-performance-target-and-layered-budgets.md). |
| Q-022 evidence acquisition method | Approved 2026-07-23 as Option B, then superseded 2026-07-26 by the native Windows Option A decision below. Preserve this remote-session choice as audit history only; operators must use the current direct-operation path. No standing remote access or credential storage is authorized. |
| Q-028/Q-029 review timing | Approved 2026-07-23 as Option C: defer trip-map usability and camera-comfort reviews until a defined visual-quality milestone. Both human gates remain open. The exact trigger was subsequently selected on 2026-07-26. |
| Q-022 native Windows operation | Approved 2026-07-26 as Option A: run Codex directly on the declared Windows PC for native RTX 3080 Ti evidence instead of using Remote Desktop or a game-streaming control path. The owner initiates the local session; no standing remote access is authorized. |
| Q-028/Q-029 integrated visual-slice trigger | Approved 2026-07-26 as Option A: perform the deferred map and camera reviews once a single review build integrates the representative Hero GT, production highway kit, and one regional environment slice, before final polish. This schedules but does not approve either human gate. |
| Q-022 Windows capture workspace | Approved 2026-07-26 as Option A: retain the pinned Windows toolchain and reusable caches, but create a fresh clean worktree at the exact reviewed commit for every reference capture. Record tool versions, clean status, and input hashes before measuring. |
| Q-030 controller bindings | Approved 2026-07-26 as Option A: retain R3 camera toggle, Y recover, and hold-LB rear view as the conflict-free controller baseline. Future remapping remains accessibility scope rather than a blocker. |
