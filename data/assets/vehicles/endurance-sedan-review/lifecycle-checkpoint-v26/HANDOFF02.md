# Lifecycle checkpoint ready for lead promotion

Both archives are byte-identical and every member has been checked for SHA-256,
size and CRC during construction and again in the separate final verification.
No engine/GPU process, canonical write or Git mutation was performed.

All output paths below are relative to `reports/p1-018/lifecycle26-retention01/`:

| Artifact | SHA-256 | Bytes |
| --- | --- | ---: |
| `lifecycle-checkpoint-v26.zip` | `8d59a307e58f5e988f2d476e73bec4f87c8e9448c5b2d1370dec1f2e8d527af3` | 54,707,049 |
| `lifecycle-checkpoint-v26-repeat.zip` | `8d59a307e58f5e988f2d476e73bec4f87c8e9448c5b2d1370dec1f2e8d527af3` | 54,707,049 |
| `manifest.json` | `e198930cc6d78133af829cb0edbe49d6b62f007513f6b9ca4b4b85fd572977ef` | 614,990 |
| `index.json` | `200bb514c451f65b0630b90b66573167869448f4085e8441d0617c07bb6176de` | 1,490 |
| `verification01.json` | `c592175bd8b200fb892e39699cf034a89da5f2e3de0fcae46b0c46e2ba12eb72` | 399 |

The ZIP holds 893 bound inputs (64,631,008 bytes), plus its exact manifest as
member 894. All 211 historical alias paths are stored as ordinary files, so the
original inventories remain directly replayable. Their declared alias targets
were checked before packaging. Five original API-digest-bound workflow artifact
ZIPs are included; all 216 nested members match their CRCs and the retained
extracted SHA/lengths. No new network acquisition was necessary.

The frozen packets include the complete failure/retry history and native logs:
earlier Mac/Linux teardown errors, original duplicate-signal errors, forced-exit
controls, wrong-tool and preparation/lint failures, the 8.1552462-second cleanup
overrun and bounded corrected failure, ineffective and corrected signal trials,
final focused Windows evidence, the full 13-step Windows M0 packet and all 20
bound input snapshots. The old incomplete long-path inventory is retained too.
Current M0 and earlier 134-per-platform F5 results remain distinct. No remote
shutdown/signal-correction pass is inferred.

All 355 declared cache omissions were rehashed (481,848,174 bytes). Extended-path
IO covers 64 paths longer than 260 characters; the longest is 296. Ten explicit
external art bindings preserve the scope of the unrelated lip review and the
brake source/full mesh payloads without duplicating those art bytes. Both actual
brake comparison PNGs, matched manifests and small proof/tool records are in
the ZIP. This remains a bounded visual review, not full asset acceptance.

The first preparation correctly rejected two later lead-owned scope-recording
helpers absent from their older frozen inventories. `failed-prepare01.py`,
`failure01.json` and `preparation-retries01.json` preserve that failure. The
correction explicitly pins those two files and the two scope audits; no
historical inventory or alias check changed. Complete packet scans have no
remaining unlisted or missing files.

`retain.py prepare`, `retain.py build` and `retain.py verify` are the recorded
commands. Preparation binds available original inputs and omissions; build uses
the immutable manifest and retained payloads only, with fixed ZIP timestamps,
ordering, permissions and compression. Verification inspects both completed
archives and rechecks the original input/omission bindings on this host.

`AUDIT01.md`, `packet-coverage01.json`, `native-zips01.json` and `members01.json`
provide the compact scope, full inventory reconciliation, nested API artifact
verification and per-member outer ZIP CRC/SHA readback. The lead owns canonical
promotion, evidence/document updates, commits and subsequent platform CI.
