# Lifecycle checkpoint scope

This checkpoint retains the frozen shutdown and signal-wait diagnosis and
correction packets, the ordinary three-platform F5 rerun, the subsequent Windows
13-step M0 run, and the bounded two-image brake-field review. It is reproducible
evidence packaging, not a clean-commit or shipping-asset reproduction claim.

The original Mac/Linux cleanup diagnostics and seven duplicate-signal errors
remain in the original API-digest-bound workflow artifacts. The five original
artifact ZIPs and their extracted evidence are retained. The 134-test result on
each platform belongs to the earlier F5 correction; it does not establish a
remote shutdown/signal-fix pass. The installed source34 asset failures remain.

The shutdown packet preserves its failed lint/tool/preparation attempts, the
earlier 8.1552462-second bookkeeping overrun, the corrected bounded failure with
unfinished bookkeeping, and the final five Windows child exits with native
observations. A late observation is not promoted to owner acceptance. The
signal packet preserves the eight-reservations/one-connection failure, staggered
timeout interference, freed-node errors, the ineffective timer observation and
closure-only intermediate, and the intentional native failed-connect control.
Final focused signal integration has seven actual network tests, three native
lifetime/connect controls and 96 host controls. The hostile test's duplicate
errors are absent in the final logs without suppression.

The retained M0 run passes all 13 steps at base
72b225ab0fd5513a16a6a6045ff27ac86b8c5d77 plus its exact working-file snapshot.
All 20 declared input hashes are bound to that run; it is not labeled a clean
committed revision. No new engine test is executed by this retention task.

Every historical payload and alias path is physically stored in this ZIP, so
the frozen inventories can be replayed without an alias reconstruction step.
Each original alias is independently checked against its declared canonical
payload. Exact shared paths are stored once. Existing cache omissions are
preserved, rehashed with Windows extended paths, and explicitly inventoried;
the previously incomplete long-path inventory is kept as failed evidence.

The brake review includes both actual original wheel PNGs and their small
source/camera/proof records. It supports a local reduction of brake face/hat
reflection lobes, with no conspicuous neighboring regression at that camera.
It does not certify all-wheel appearance, motion, export or runtime. Large art
source/mesh payloads and the unrelated lip comparison media remain explicit
external bindings, already owned by the lead's art retention. They are not
silently dropped from the historical record or treated as included payloads.

All writes are confined to reports/p1-018/lifecycle26-retention01. No canonical
files, source scenes, exports, Git state, workflows or approval records change.
Human approval remains null. Final Mac/Linux correction reruns, final vehicle
source/export/runtime and human gates remain separate.
